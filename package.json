{
  "name": "cloudflare-ip-update",
  "version": "1.0.0",
  "description": "Check and update Cloudflare DNS A records to match current public IP for multiple domains.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.0.0"
  }
}
